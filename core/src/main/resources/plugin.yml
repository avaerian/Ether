# TODO: refactor so this can be autogenerated?
name: Ether
version: 0.0.0
api-version: 1.19
main: org.minerift.ether.EtherPlugin
author: Avaerian
commands:
  nmschunk:
    description: Debug command for clearing chunks via NMS
  nmsblock:
    description: Debug command for setting chunks via NMS
  pasteschem:
    description: Debug command for pasting a schematic
  blockscan:
    description: Debug command for scanning islands for blocks/levels
  cfgreload:
    description: Debug command for reloading the main config
  island:
    description: Debug command for island stuff
  testreg:
    description: Debug command for NMS registry testing
  dbgnbt:
    description: Debug command for item, nbt, and island spec testing
  #nmsentity:
  #  description: Debug command for spawning an entity via NMS
softdepend: [WorldEdit]